<h1><%= @questions.count %> Questions</h1>

<div id="questions">
	<% @questions.each do |question| %>
		<%= div_for question do %>
		<%= question.answers_count %> answers
		<%= question.money %> Yuan
		<%= question.credit %> Credits
		<%= link_to question.title, question %>
		<% end %>
	<% end %>
</div>

<%= link_to 'New Question', new_question_path %>
